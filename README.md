# whitespace-stego
A steganography toolkit that hides messages using invisible Unicode whitespace characters (like zero-width space, non-breaking space, etc.) within text. Includes Python and Rust implementations, a pluggable CLI, and support for encoding, decoding, and detection.
